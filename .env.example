# Common settings
TIMEZONE="Australia/Adelaide"

# For server deployment (docker-compose-server.yml)
COMPOSE_FILE=docker-compose-server.yml
HOSTNAME=pimento.home.kerr.host
# PI_HOLE_API_TOKEN=

# For node deployment (docker-compose-node.yml)
# COMPOSE_FILE=docker-compose-node.yml
# TAILSCALE_IP=100.x.x.x        # Required: This host's Tailscale IP
# CADVISOR_PORT=8080            # Optional: Defaults to 8080, set to 8081 if port conflict
